from pathlib import Path


class RarediseaseParamsCreator:

    def create(self, case_id: str, case_path: Path, dry_run: bool = False) -> None:
        """Create a params file for the raredisease pipeline."""
        pass
